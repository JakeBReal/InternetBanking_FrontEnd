document.getElementById('transferir').addEventListener('click', function() {
    alert('Simulando la transferencia...');
    // Aquí podrías agregar código para procesar la transferencia (si tuvieras un backend)
});